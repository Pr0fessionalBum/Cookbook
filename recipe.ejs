<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= recipe.name %> - Recipe App</title>
    <link rel="stylesheet" href="./styles.css">
    <script src="./script.js" defer></script>
</head>
<body>
    <%- include('../partials/header') %>
    <h1><%= recipe.name %></h1>
    <p><strong>Origin:</strong> <%= recipe.origin %></p>
    <p><strong>Safety Tips:</strong> <%= recipe.safetyTips %></p>
    <p><strong>Cook Time:</strong> <%= recipe.cookTime %> minutes</p>
    <h2>Ingredients</h2>
    <ul>
        <% ingredients.forEach(ingredient => { %>
            <li class="ingredient"><%= ingredient.name %>
                <div class="popup">
                    <p><strong>Origin:</strong> <%= ingredient.origin %></p>
                    <p><strong>Safety Tips:</strong> <%= ingredient.safetyTips %></p>
                </div>
            </li>
        <% }) %>
    </ul>
    <%- include('../partials/footer') %>
</body>
</html>