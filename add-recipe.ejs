<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Recipe - Recipe App</title>
    <link rel="stylesheet" href="./styles.css">
</head>
<body>
    <%- include('../partials/header') %>
    <h1>Add Recipe</h1>
    <nav>
        <a href="home.ejs">Home</a> | 
        <a href="recipeList.ejs">Recipes</a> | 
        <a href="add-recipe.ejs">Add Recipe</a>
      </nav>
    <form action="/add-recipe" method="POST">
        <div>
            <label for="name">Recipe Name:</label>
            <input type="text" id="name" name="name" required>
        </div>
        <div>
            <label for="origin">Origin:</label>
            <input type="text" id="origin" name="origin" required>
        </div>
        <div>
            <label for="safetyTips">Safety Tips:</label>
            <textarea id="safetyTips" name="safetyTips"></textarea>
        </div>
        <div>
            <label for="cookTime">Cook Time (minutes):</label>
            <input type="number" id="cookTime" name="cookTime" required>
        </div>
        <div>
            <label for="ingredients">Ingredients:</label>
            <select id="ingredients" name="ingredients[]" multiple required>
                <% ingredients.forEach(ingredient => { %>
                    <option value="<%= ingredient.id %>"><%= ingredient.name %></option>
                <% }) %>
            </select>
        </div>
        <button type="submit">Add Recipe</button>
    </form>
    <%- include('../partials/footer') %>
</body>
</html>