<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Recipes</title>
  <link rel="stylesheet" href="../Public/css/recipestyle.css">
</head>
<body>
  <div class="container">
    <h1>Recipes</h1>
    <nav>
      <a href="index.ejs">Home</a> | 
      <a href="recipes.ejs">Recipes</a> | 
      <a href="add-recipe.ejs">Add Recipe</a>
    </nav>
    <% for (let protein in categorizedRecipes) { %>
      <h2><%= protein %></h2>
      <ul>
        <% categorizedRecipes[protein].forEach(recipe => { %>
          <li><a href="recipes.js/<%= recipe.id %>"><%= recipe.name %></a></li>
        <% }) %>
      </ul>
    <% } %>
  </div>
</body>
</html>